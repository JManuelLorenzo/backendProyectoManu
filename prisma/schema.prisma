datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Quote {
  ID        Int     @id @default(autoincrement())
  movie     String
  quote     String
  character String
  deleted Boolean @default(false)
}
